using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class weaponAmmoCooldown : MonoBehaviour
{
    // Start is called before the first frame update
    float counter;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
